export const metadata = {
  title: 'MDX 블로그 시작하기',
  description: 'DB 없이 MDX 파일만으로 운영하는 블로그의 첫 글 예시입니다.',
  date: '2026-06-14',
  tags: ['Next.js', 'MDX'],
};

이 글은 `src/content/posts` 디렉터리에 글을 추가하는 방식을 보여주는 예시입니다.

## 글 작성 규칙

각 글은 글 폴더 안의 `index.mdx`와 `metadata` export로 구성됩니다.

```mdx
export const metadata = {
  title: '글 제목',
  description: '목록과 메타데이터에 사용할 짧은 설명',
  date: '2026-06-14',
  tags: ['Next.js'],
};

본문...
```

## MDX 사용

본문에서는 일반 Markdown 문법과 React 컴포넌트를 함께 사용할 수 있습니다.
공통 MDX 컴포넌트는 `src/mdx-components.tsx`에서 관리합니다.
